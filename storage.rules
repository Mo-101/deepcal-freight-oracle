
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /training-data/{allPaths=**} {
      allow read, write: if true; // Adjust based on your auth requirements
    }
    
    match /model-weights/{allPaths=**} {
      allow read, write: if true; // Adjust based on your auth requirements
    }
  }
}
